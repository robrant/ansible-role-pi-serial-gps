# Include python bindings
install_python_binding: true

# Test gps data being received
test_data_input: true

#  Which version of python gpsd to install
python_gpsd_version: 0.33.3

# Which version of the Raspberry Pi hardware?
# ** UNTESTED **
pi_major_version: 1

# Whether to run the gps data check
run_gps_data_check: true
